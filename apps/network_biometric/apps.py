
from django.apps import AppConfig

class NetworkBiometricConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.network_biometric' # <-- Must match the path from the project root